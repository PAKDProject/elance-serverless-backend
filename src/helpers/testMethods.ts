import { isBlacklisted } from "../lib/userCheck";

export const testMethods = () => {
    /* testing isblacklisted
    isBlacklisted("test-token").then(val => {
        throw new Error("Result " + val)
    }).catch(error => {
        throw (error)
    }) */
}